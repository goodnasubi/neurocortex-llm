[pytest]
# ステップ44で追加した Python スクリプト（step44_figure_template.py など）は
# テスト対象ではないため、testpaths で tests/ のみに明示的に限定

testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# 最大実行時間・並列実行などの設定は必要に応じて追加
