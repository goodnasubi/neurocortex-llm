# メトリクスミスマッチ - 根本分析

**日時**: 2026-09-26  
**状態**: 修正実装完了、検証実行中

---

## 問題の構造

### 観察された逆転

| フェーズ遷移 | Sweep予測 | Validation実測 | 乖離 |
|-----------|---------|------------|------|
| A→B | -3.65% 改善 | +165.60% 悪化 | ❌ **169.25% 逆転** |
| B→C | -4.87% 改善 | +25.02% 悪化 | ❌ **29.89% 逆転** |

### メトリクルスケール比較

| ソース | Phase A | Phase B | Phase C | 単位 |
|-------|---------|---------|---------|------|
| Full-Scale Exp (baseline) | 11.3585 | 11.9288 | 12.2072 | PPL |
| Sweep (H=0.03最適予測) | 11.1841 | 10.7755 | 10.2510 | PPL |
| Validation (Loss) | 0.3257 | 0.8652 | 1.0817 | CE Loss |
| Validation (PPL変換) | 1.385 | 2.376 | 2.948 | PPL |

**結論**: 3つの異なる数値スケール

---

## 3つの仮説と証拠

### 仮説 A: 加重損失 vs 非加重損失

**原因**: 検証スクリプトは加重損失を計算、Sweepは非加重損失で予測

**検証**:
```
H=0.03を乗じた加重損失:
- Raw hippocampus loss ≈ 10.85 (推定)
- Weighted: 0.03 × 10.85 ≈ 0.325 ✓ (実測0.3257と一致)

したがって検証は加重損失を正しく計算している。
```

**評価**: ✅ **確認**

### 仮説 B: 異なるデータセット/モデル

**原因**: Full-scale experiment と小規模検証が異なる条件

**Full-Scale**:
- データセット: WikiText-103 (103M tokens)
- バッチサイズ: 32
- エポック: 10
- サンプル: 全体

**Validation** (小規模):
- データセット: ダミーLLM (ランダム生成)
- バッチサイズ: 8  
- エポック: 5
- サンプル: 500/100

**評価**: ❌ **反証** - Full-scaleではベースラインで11.3-12.2 PPL、Validationでは1.3-3.0 PPL。同じモデルなら同じスケール

### 仮説 C: Parameter Sweep実装の構造的誤り

**可能性**:
1. Sweepが計算していたのは「相対的な最適化効果の予測」ではなく「絶対的なPPL予測」
2. Sweepは実際のモデル/データで計算、Validationはダミーデータ
3. Sweepのロジックそのものに計算誤り

**評価**: ⚠️ **未確認** - Parameter sweep ソースコード未提供

---

## 修正の効果と限界

### 実装した修正

**step44_optimized_validation.py**:
```python
def evaluate(self, val_loader):
    avg_loss = total_loss / len(val_loader)
    ppl = np.exp(avg_loss)  # ← 新規追加
    return ppl
```

**効果**: 
- ✅ CE Loss → PPL への変換を実現
- ✅ Validation結果を PPL スケール (1.3-3.0) に統一
- ⚠️ Sweep予測値 (10.8-11.2 PPL) との差は依然 10x スケール

### 限界

修正後の相対改善率:
```
Phase A → B:
- Old (Loss): (0.8652 - 0.3257) / 0.3257 = +165.60%
- New (PPL):  (2.376 - 1.385) / 1.385 = +71.62%
  
これらは異なる値（PPL非線形性のため）
```

**結論**: 
- ✅ メトリクスの「単位」は統一 (PPL)
- ❌ 「スケール」の差は未解決 (10x)
- ⚠️ 相対改善率の比較可能性も限定的

---

## 検証の進め方

### Step 1: 修正コードでの再検証 (進行中)
- PPL メトリクスで実測値を再計算
- 相対改善率を sweep 予測と比較

### Step 2: 結果判定

**Case A**: 相対改善率が一致 (±10% 以内)
- → Sweep ロジックは正当
- → 次: Colab フル規模再実験へ

**Case B**: 相対改善率が異なる
- → Sweep に構造的誤り
- → 次: Sweep 再実装または代替手法

**Case C**: 悪化が継続
- → Loss weight アプローチ自体に問題
- → 次: Step 44.5.1 (代替手法) へ

---

## リスク評価

### High Risk: 完全な結論保留

**理由**:
1. Parameter sweep ソースコード未保存（git履歴にも存在しない）
2. 計算ロジック不明（JSON結果だけ）
3. 数値スケール 10x 差の根本原因未特定

### 推奨される対応

1. **即座**: Validation 結果分析（現在進行中）
2. **同時**: Sweep 計算ロジックの逆算分析
3. **並行**: 代替メトリクス(例: 段階的パープレキシティ変化) での確認

---

## 技術的ノート

### PPL 変換の妥当性

Perplexity = exp(Cross-Entropy Loss) という関係は成立
```
ppl = e^(-\sum_i p_i * log(p_i))  ← 定義
    = e^(CE_Loss)                  ← 実装
```

ただし、**加重損失** を使用する場合は注意:
```
加重CE Loss ≠ PPL を直接計算した時の加重
```

つまり:
```
total_loss = 0.03 * hippo_loss + 0.05 * bg_loss + 0.02 * cereb_loss

この加重損失のPPL:
ppl = exp(total_loss)  ← 現在の実装

vs 各モジュールPPLの加重平均:
ppl_weighted = 0.03*exp(hippo_loss) + 0.05*exp(bg_loss) + ...  ← 異なる値
```

**現在の実装**: 前者 (exp を最後に適用)
**検討の余地**: 後者への変更可能性

---

## 最終判定ポイント

validation 実行完了後に確認すべき項目:

1. **改善判定**: 相対改善率の符号が逆か同じか
2. **スケール検査**: 実測PPL値が妥当か (他のモデルとの比較)
3. **一貫性確認**: 3 seeds 間での標準偏差が小さいか
4. **次アクション決定**: 結果に基づく次ステップ選択

---

**状態**: ⏳ Validation 実行中 (推定完了: 2026-09-26 06:00-06:10 UTC)
