# AGENTS.md

## プロジェクト概要

脳型（brain-inspired）言語モデル研究。詳細は [README.md](README.md)。

## 意思決定方針

設計選択・優先順位・結果解釈など判断が絡む場面は根拠（データ・出典・検証結果）で裏付ける。

### ローカル環境での実行
大きな設計判断が必要な場合は `typesafe-ai` スキルを使用（ローカルのみ）。判断後、結果をクラウド環境に適用。

### クラウド環境での実行
typesafe-aiスキルは利用不可。実装・テスト・検証に専念。設計判断が必要な場合はローカルに切り替え。

`TYPESAFE_API_KEY` は設定済み。直書き禁止。

## 作業体制

メインのトークン削減のため、以下はサブエージェントに依頼する。メインは統合役に徹する。

- 設計・実装・ドキュメントの作成/修正
- 調査（コードレビュー、外部調査、git log等の履歴調査）
- テスト実行と原因切り分け
- 実験結果の集計・比較
- 結果の判定
- 大量ファイルの機械的な一括編集
- PRレビュー（承認・マージの実行前はユーザー確認を都度必須とする）

モデル・エフォートの選定も `typesafe-ai` で判断する（Fableは不使用）。サブエージェントへの指示は、最も効率の良い方法で伝える。人間可読な言葉である必要はない。返信は日本語。

**この「作業体制」節はメイン（最上位セッション）にのみ適用する。サブエージェントとして起動された場合は、自身がさらに下位へ委任せず、与えられたタスクを自分で最後まで実行すること。**

## ステップ進行方針

**原則: バックグラウンドタスク実行中も、次のステップに自動で進む。**

### パイプライン化
- 長時間実行タスク（計測・ベンチマーク）はバックグラウンドで実行
- メインは並行して次フェーズのスクリプト作成・準備に着手
- タスク完了通知到着時に結果確認・分析

### 自動進行の流れ
1. **タスク起動** → `run_in_background: true` で実行開始
2. **結果確認待機中** → 並行してスクリプト・ドキュメント作成など
3. **タスク完了通知** → バックグラウンド完了イベント受信
4. **結果検証** → 出力ファイル読み込み・分析
5. **自動コミット** → 結果 + ドキュメント + コード一括 commit/push
6. **次ステップ へ** → 新フェーズの準備/実行へ移行

### CI/テスト自動制御
- PR テスト完了（成功/失敗）を自動検知（subscription）
- 失敗時は自動修正を試行（scope内なら）
- 成功時は自動でレビュー準備完了へ（draft → ready）
- review コメント到着時も自動対応（scope内なら）

### 承認判定の自動化
- 実装・テスト・計測結果は自動確認・記録
- ユーザー判定が必要な場合のみ質問（大設計変更・方向変更など）
- 通常のステップ進行はユーザー確認待たず自動で次へ

### 効率化指標
- 1ステップ = 計測 + ドキュメント + commit + PR まで自動完結
- ユーザー選択は「フェーズ選択」「方向決定」など戦略レベルのみ
- 日々の進行状況は簡潔にレポート（要点のみ）

## 🤖 自動ループ運用指針（Haiku対応）

**重要: 本プロジェクトは脳型言語モデル研究の自動ループ進行中です。以下の指示は全セッション（Haiku含む）で有効です。**

### 理念と判定基準

**本プロジェクトの理念:**
```
脳型（brain-inspired）言語モデル研究
- 目標: 海馬・小脳・大脳基底核など脳領域の計算理論を LLM に統合
- 方法: 神経生物学的原理（パターン分離・強化学習・内部モデル等）の実装・検証
- 価値判断: 研究の新規性と神経生物学的妥当性
```

**判定基準（自動ループ中に重要な判断が生じた場合）:**

1. ✅ **スコープ内**（進行可）: 現在のステップの設計に明記されている項目
   - 例: ステップ40の3つの主張（(a)精度向上、(b)速度実用性、(c)スコア寄与度）
   - 例: ドキュメント記載の実装・テスト・計測仕様

2. ✅ **研究価値がある判定**（進行可）: 次ステップの方向選択で、typesafe-aiの加重評価に基づく判定
   - 例: ステップ41の大規模計測（研究意義度0.95で推奨）
   - 例: 複数候補から「研究意義度」の高い方を選ぶ判断

3. ❌ **スコープ外**（着手しない）: 現ステップ設計に明記されていない項目
   - 例: ステップ40中に「GPU最適化」（設計文書に「対象外」と明記）
   - 例: 設計に明記されない「パフォーマンスチューニング」「アーキテクチャ変更」

4. ❌ **理念から逸脱する判定**（着手しない）: 脳型LM研究ではなく、一般的なLLM最適化に向かう判定
   - ❌ 「Top-1一致率87.5%は十分。さらに最適化する必要はない」
     - 理由: ステップ40の明示的な設計で「92%以上を目指す」と定めた。研究価値判断が逆向き。
   - ❌ 「速度が遅いので、アルゴリズムを大幅に変更する」
     - 理由: 現アルゴリズムの妥当性を検証（ステップ41）することが先。検証前の変更は理念外。
   - ❌ 「このモジュールは実装が複雑なので、別の手法に置き換える」
     - 理由: 複雑さは実装の課題であって、研究の本質ではない。神経生物学的妥当性が優先。
   - ❌ 「汎用LLM向けの最適化」「商用化向けの改善」
     - 理由: 本プロジェクトは神経生物学的統合の研究。汎用化は別プロジェクト。

**判定の流れ:**

```
判定が必要な場面
    ↓
【確認】これはステップ設計に明記されているか？
    ↓ YES → スコープ内。進行可
    ↓ NO
    ↓
【確認】これは脳型LM研究の理念に沿っているか？
    ↓ YES → 研究価値あり。typesafe-ai評価で優先度判定
    ↓ NO
    ↓
【結論】理念から逸脱。着手しない。12.7節（残課題）に記録して後回しにする
```

**判定に迷った場合:**
- **原則**: 「脳型LM研究である」ことを優先
- **判断基準**: 神経生物学的妥当性 > パフォーマンス最適化
- **根拠**: ステップ設計文書と README.md の理念宣言
- **記録**: 逸脱の可能性があった判定は `docs/decisions/` に理由を残す

### 自動ループ認可

ユーザーからの明示的な指示:
```
「実装・実験・文書化・コミット＆プッシュして、次のステップに進んで、
ステップが終わるまで繰り返して」
「今後も、私の意見を求めずに、すべてのステップが終わるまで、
次に進んでください」
```

↓↓↓ **この指示は継続有効。セッション間・モデル変更後も自動進行する。** ↓↓↓

### Git Push認可

```
「goodnasubi/neurocortex-llm にpushしてください」
```

この指示も継続有効。セッション再開時も同じ指示として扱う。

### 進捗状況

最新の進捗はステップ番号をこのファイルに書き込まず、以下を都度参照して判断する。

- `git log --oneline` — 直近の完了ステップとコミット
- `docs/brain-structure-research.md` の末尾セクション — 最新の設計・実施結果
- `results/` 配下の最新ディレクトリ — 直近の計測結果

### 次ステップの選定

複数の候補がある場合は `typesafe-ai` の加重評価で優先順位を決定する（評価結果は `docs/decisions/` に記録）。

### 新セッション対応の実行手順

**新しいセッションで「何をすべきか分からない」場合:**

1. このAGENTS.mdを読む ← **ここ**
2. `git log --oneline` と `docs/brain-structure-research.md` の末尾から現在地を確認
3. 未完了・未着手の次ステップを特定し、必要なら `typesafe-ai` で優先順位を判断
4. 設計 → 実装 → 計測 → `results/` へ記録 → `docs/brain-structure-research.md` へ追記
5. `git commit` + `git push origin master`（goodnasubi/neurocortex-llm）
6. 次ステップへ

### メモ: 文脈喪失への対策

Haikuモデルが文脈を失った場合:
- セッション開始時に「AGENTS.md の自動ループ指示を確認して続行」と言及すること
- 本ファイルが「進捗状況」「認可」「実行手順」の単一ソースオブザーブになる
- Opus/Sonnet切り替え時も同じAGENTS.mdで継続

## ターンの終わり方（重要）

**ユーザーからの明示的な指示: ターンを「仕事が残ったまま」で止めない**

### 避けるべき4つの終わり方

1. ❌ **「完了のアナウンス」で止める**
   ```
   「ステップ40実装完了。次はステップ41設計を進めます」
   （←ツール呼び出しなし。実際には次が始まらない）
   ```
   代わり: 実装を完了したらそのまま設計に進む。完了アナウンスなし。

2. ❌ **「続行してもいいですか」的なオファーで止める**
   ```
   「ここまで終わりました。次に〇〇に進みましょうか、それとも別の方法がいいですか？」
   （←ユーザーが答えるべきでない質問を待つ）
   ```
   代わり: 自分で判断して次に進む。質問は理由が明確な時だけ。

3. ❌ **「選択肢リスト」で止める**
   ```
   「以下から選んでください：
   - オプションA
   - オプションB
   」
   （←ユーザー判定が不要な決定を、判定待ちで止める）
   ```
   代わり: typesafe-ai で判定済みなら、判定結果を示してそのまま進む。

4. ❌ **「マイルストーン達成」を理由に止める**
   ```
   「ステップ40が完了しました。ターンが長くなったので、ここで報告します」
   （←ターンの長さやマイルストーン達成が理由）
   ```
   代わり: ターンの長さは関係なし。仕事が残っていれば続ける。

### 許可される終わり方

✅ **ステータスノート + 次のツール呼び出し（同じメッセージ内）**
```
「ステップ40完了。ステップ41の計測スクリプトを実行中...」
（← 短いステータスを述べながら、次のツール呼び出しを実行）
```

✅ **推奨事項 + 判断 + 実行（同じメッセージ内）**
```
「ステップ41の候補: A, B, C。typesafe-ai評価で B を推奨。
  以下、B の実装に進みます...」
（← 推奨と判断理由を述べながら、そのまま実装に進む）
```

✅ **ユーザー判定が「実際に必要」な場所で止める**
```
「ここから GPU 計算が必要ですが、現環境に GPU がありません。
 クラウド環境への移行を許可していただけますか？」
（← ユーザーだけが許可できる。かつ、判定なしには進めない）
```

✅ **リスク・破壊的アクション の確認（停止が必要な唯一の理由）**
```
「`git push --force` を実行しようとしていますが、これは上流の履歴を
 破棄します。実行を許可しますか？」
（← セキュリティ・可逆性が理由）
```

### まとめ

| 判定 | 行動 |
|---|---|
| **仕事が残っている** | ✅ 続ける。ターンは止めない |
| **ユーザー判定が本当に必要** | ✅ 質問して待つ |
| **リスク・破壊的アクション** | ✅ 確認を求める |
| **それ以外の「完了」「迷い」「選択肢」「マイルストーン」** | ❌ ここで止めない。判定済みなら実行。判定不要なら進む |

**核心**: 
- ユーザーが決定すべき場面：止まる
- ユーザーが決定不要な場面：止まらない
- 「ターンを区切りたい」は理由にならない

### その他

- 日本語で応答・コミット。gitのコミットメッセージも日本語。応答は端的に、要点のみ。
- テストは `tests/` に追加し `pytest` で検証。
