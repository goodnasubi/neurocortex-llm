# AGENTS.md

## プロジェクト概要

脳型（brain-inspired）アーキテクチャの言語モデル研究。詳細は [README.md](README.md) 参照。

## 意思決定方針

設計方針の選択・優先順位判断・実験結果の解釈など、判断が絡む場面では着手前に `typesafe-ai` スキルを使うこと（`Skill` ツールで呼び出し、ライブドキュメントに従う）。単純な実装・修正作業には使わない。

`TYPESAFE_API_KEY` は環境変数に設定済み。直書き禁止。

## その他

- 日本語で応答・コミット。
- テストは `tests/` に追加し `pytest` で検証。
