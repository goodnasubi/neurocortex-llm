# AGENTS.md

## プロジェクト

脳型（brain-inspired）言語モデル研究。詳細は [README.md](README.md)。

**理念**: 海馬・小脳・大脳基底核などの計算理論をLMに統合し、神経生物学的原理を実装・検証する。価値基準は「研究の新規性」と「神経生物学的妥当性」。

## 意思決定

- 判断（設計選択・優先順位・結果解釈）は根拠（データ・出典・検証結果）で裏付ける。
- 大きな判断・候補選定は `typesafe-ai` の加重評価で行い、結果は `docs/decisions/` に記録（ローカルのみ利用可。クラウドでは実装・検証に専念）。
- `TYPESAFE_API_KEY` は設定済み。直書き禁止。

### 判定基準

1. ステップ設計に明記 → 進める
2. 設計外だが理念に沿う → typesafe-ai で優先度判定
3. 設計外 → 着手せず 12.7節（残課題）に記録
4. 理念から逸脱（汎用LLM最適化・商用化・検証前の手法差し替え・設計目標の独断的な引き下げ）→ 着手しない

迷ったら「神経生物学的妥当性 > 性能最適化」。

## 作業体制（メインセッションのみ）

メインは統合役に徹し、以下はサブエージェントに依頼する:
設計・実装・ドキュメント作成/修正、調査（コードレビュー・外部調査・git履歴）、テスト実行と原因切り分け、実験結果の集計・比較・判定、大量ファイルの一括編集、PRレビュー（承認・マージ前は毎回ユーザー確認）。

- モデル・エフォートは `typesafe-ai` で選定（Fable不使用）。
- 指示は最も効率の良い形式で（人間可読である必要なし）。返信は日本語。
- サブエージェントとして起動された場合は、さらに下位へ委任せず自分で完遂する。

## 自動ループ（継続有効）

ユーザー指示（セッション・モデルが変わっても有効）:
- 「実装・実験・文書化・コミット＆プッシュして、次のステップに進んで、ステップが終わるまで繰り返して」
- 「私の意見を求めずに、すべてのステップが終わるまで次に進んでください」
- 「goodnasubi/neurocortex-llm にpushしてください」

### 1ステップの流れ

1. 現在地確認: `git log --oneline`、`docs/brain-structure-research.md` 末尾、`results/` 最新
2. 次ステップ選定（候補複数なら typesafe-ai）
3. 設計を docs に追記 → 実装 → テスト（`tests/`、`pytest`）
4. 計測（長時間は `run_in_background`、待機中に次の準備を並行）→ `results/` に保存
5. 結果を docs に追記 → commit → `git push origin master`
6. 次ステップへ

- 進捗（ステップ番号など）は本ファイルに書かず、上記ソースを都度参照する。
- CI: 失敗はスコープ内なら自動修正、成功なら draft → ready、レビューコメントもスコープ内なら自動対応。

## ターンの終え方

仕事が残っている限りターンを終えない。ステータスや推奨は、次のツール呼び出しと同じメッセージに書いて続行する。

止まってよいのは次の場合のみ:
- ユーザーにしか決められず、それなしでは進めない
- 意図的に保護された対象に阻まれている
- リスク・破壊的操作の確認が必要

以下を理由に止まらない: 完了報告と次の予告だけで終わる／「続けましょうか」と尋ねる／作業を止めない決定事項を列挙して待つ／ターンが長い・区切りがよい。

## その他

- 応答・コミットメッセージは日本語。応答は端的に要点のみ。
