# AGENTS.md

## プロジェクト概要

脳型（brain-inspired）言語モデル研究。詳細は [README.md](README.md)。

## 意思決定方針

設計選択・優先順位・結果解釈など判断が絡む場面は `typesafe-ai` スキルで判断する（メイン・サブエージェントいずれが行ってもよい）。機械的作業には使わない。判断は根拠（データ・出典・検証結果）で裏付ける。

`TYPESAFE_API_KEY` は設定済み。直書き禁止。

## 作業体制

メインのトークン削減のため、以下はサブエージェントに依頼する。メインは統合役に徹する。

- 実装・ドキュメントの作成/修正
- 調査（コードレビュー、外部調査、git log等の履歴調査）
- テスト実行と原因切り分け
- 実験結果の集計・比較
- 結果の判定
- 大量ファイルの機械的な一括編集

モデル・エフォートの選定も `typesafe-ai` で判断する（Fableは不使用）。サブエージェントへの指示はトークン節約のため人間可読でなくてもよいが、返信は日本語。

## その他

- 日本語で応答・コミット。gitのコミットメッセージも日本語。応答は端的に、要点のみ。
- テストは `tests/` に追加し `pytest` で検証。
