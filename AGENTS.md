# AGENTS.md

## プロジェクト概要

脳型（brain-inspired）言語モデル研究。詳細は [README.md](README.md)。

## 意思決定方針

設計選択・優先順位・結果解釈など判断が絡む場面は `typesafe-ai` スキルで判断する。機械的作業には使わない。判断は根拠（データ・出典・検証結果）で裏付ける。

`TYPESAFE_API_KEY` は設定済み。直書き禁止。

## 作業体制

実装・調査はサブエージェントに依頼し、メインは結果の判定・統合に徹する。ソースコードの作成・修正は、メインのトークン削減のため必ずサブエージェントで行う。ただし軽微な編集（数行程度のドキュメント修正等）は例外としてメインが直接行ってよい。モデル・エフォートの選定も `typesafe-ai` で判断する（Fableは不使用）。サブエージェントへの指示はトークン節約のため人間可読でなくてもよいが、返信は日本語。

## その他

- 日本語で応答・コミット。応答は端的に、要点のみ。
- テストは `tests/` に追加し `pytest` で検証。
